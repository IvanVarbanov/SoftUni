function triangle(number) {
    for (let i = 1; i <= number; i++) {
        let result = "";
        for (let j = 0; j < i; j++) {
            result += `${i} `;
        }
        console.log(result);

        // dsfdsfoisdj dsoifjdsoif
    }
}

triangle(3);
triangle(5);
triangle(6);
